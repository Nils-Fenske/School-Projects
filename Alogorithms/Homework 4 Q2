void pos_neg_Partition(int* A[], int p) {
	int x = A[r];
	int i = p - 1;
	int temp = null;

	for(int j = p, j < r, j++){
		if (A[j] >= 0){
			i++:
			temp = A[i];
			A[i] = A[j];
			A[j] = temp;
		}
	}
	temp = A[i + 1];
	A[i + 1] = A[r];
	A[r] = A[temp];
}